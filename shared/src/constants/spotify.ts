/** Spotify API batch limit for /audio-features endpoint (deprecated — blocked for new dev apps). */
export const SPOTIFY_AUDIO_FEATURES_BATCH_SIZE = 100;

export const SPOTIFY_TOP_TRACKS_LIMIT = 50;

/** Feb 2026: GET /search limit max is 10. */
export const SPOTIFY_SEARCH_LIMIT = 10;
